- **Date**: 2026-06-17
- **Agent**: claude-opus-4-8[1m] (main conversation; advocatus-diaboli subagents for spec- and code-mode review)
- **Task**: Drove the harness-affordances epic steps 3-7 (#200 section + add, #201 chained constraints, #202 review + staleness, #203 runtime recorder) through full spec-first + dual-diaboli, plus an earlier run of bug clusters (#361, #398 reflection fragments, #319/#320 verify_rhs, #322-#325 + #424 auto-enforcer) and the microsoft/AI-Engineering-Coach exploration (#340 → R1-R5 issues).
- **Surprise**: The code-mode `/diaboli` on deterministic bash scripts repeatedly caught a class of BSD-vs-GNU portability bugs that all passed local macOS tests. The sharpest: `grep '^OK\|^DIAG'` — GNU `\|` alternation is a *literal* under BSD grep, so the gate would silently self-skip a SAFETY constraint on a clean macOS/CI box — and it passed locally only because the Claude Code harness aliases `grep` to `ugrep`, masking the bug entirely (confirmed by re-running with `/usr/bin/grep`). The same review surfaced `date -u -j -f '%Y-%m-%d'` on BSD filling the time-of-day from localtime-*now* (not UTC midnight), breaking a "machine-independent" age computation; and in #203 the privacy guarantee was *declared but not enforced* (a Bash command's first word could be `VAR=secret` or a path) until the diaboli forced env-prefix stripping + basename + shape-allowlist. The recurrence across three PRs (#201, #202, #203) is the signal, not any single bug.
- **Proposal**: AGENTS.md GOTCHA — "BSD/GNU shell portability traps in deterministic check scripts." Concretely: `grep '\|'` is BSD-literal (and the harness aliases `grep`→`ugrep`, so it passes locally — verify portability with `/usr/bin/grep`); `date -u -j -f '%Y-%m-%d'` does NOT pin UTC midnight on BSD (parse `YYYY-MM-DD 00:00:00` explicitly and round-trip-validate); `stat -f%z` (BSD) vs `-c%s` (GNU); pin `sort` with `LC_ALL=C`. And: a privacy/safety guarantee in a recorder must be *enforced by code* (sanitise/allowlist, fail to null), never merely declared.
- **Improvement**: Run the Layer 0 bash suite on a macOS + Ubuntu CI matrix so BSD/GNU divergence surfaces deterministically instead of depending on adversarial review or remembering to run `/usr/bin/grep`. (Declared as the `Layer 0 bash tests run on macOS and Linux` constraint below; implementation — adding `macos-latest` to `tdad-tests-fast.yml` — is the follow-up that promotes it from `unverified`.)
- **Signal**: failure
- **Constraint**: Layer 0 bash tests run on macOS and Linux (unverified — declared in HARNESS.md, awaiting a `macos-latest` CI-matrix leg)
- **Session metadata**:
  - Duration: long multi-hour session (2026-06-15 → 2026-06-17) spanning ~14 merged PRs
  - Model tiers used: claude-opus-4-8[1m] for the main driving/design loop throughout; advocatus-diaboli subagents (spec- and code-mode) dispatched per affordance step. Main-loop-dominant; exact split unknown.
  - Pipeline stages completed: not an orchestrator run — manual per-step spec-first: spec (commit 1) → spec-mode diaboli → user-adjudicated forks → implement → code-mode diaboli → adjudicate/harden → version+CHANGELOG → PR → CI → merge.
  - Agent delegation: partial — diaboli subagents dispatched per step; spec-writing, implementation, integration driven by hand.
