- **Date**: 2026-08-25
- **Agent**: Claude Opus 5 (1M context), interactive session
- **Task**: Retired the harness evolution loop in full. Ran `/harness-assay` on the #602/#605/#606 cluster, took its P1 finding through `/diaboli` (12 objections: 4 accepted, 8 moot), wrote an errata retiring that finding, re-scoped #606 twice — both times wrongly — wrote a spec for a no-generated-region design that returned 1 critical and 7 high, and then reverted the whole apparatus. PR #611: 105 files, 563 insertions, 16,673 deletions, v0.91.0. Removed 8 commands, 2 agents, 1 skill, 3 scripts, the `harness/` corpus, 2 constraints, 13 Layer 0 tests, 11 TDAD scenarios and 9 docs pages. `HARNESS.md` is now the human-curated master, fed by the reflection channel and reflected outward by `/harness-sync`.
- **Surprise**: The machinery could refine indefinitely and never terminate, and retiring it was cheaper than any rule change it governed. Every adversarial pass worked as designed — each returned real criticals, each made the design narrower and more honest. **None could ask whether the thing being refined should exist.** Assay finding-1 cleared a full `/diaboli` pass and then died to one `git log -S` command a human ran, because its stated harm was false. The successor spec absorbed that lesson, was reviewed, and returned a critical showing its own justifying dichotomy named artefacts the compiler never wrote to. Three positions on the same evidence in one session, each better argued than the last, none converging. The arithmetic is the sharp part: removing one version marker through the loop cost five spec revisions, three adversarial reviews, a choice-story pass and twenty-four objection dispositions; removing the loop itself cost one spec and one PR. A governance mechanism more expensive to change a rule with than to delete outright had inverted its own purpose — and nothing inside it could report that, because every component was working correctly.
- **Proposal**: WORKFLOW — when a decision produces successive revisions that each survive adversarial review and still do not converge, treat the non-convergence as evidence about the **mechanism**, not the design. Three rounds of criticals read naturally as "the spec isn't good enough yet"; the reading available after round two was "the thing this spec is defending may not be worth defending." A review pipeline can tell you a design is wrong. It cannot tell you the problem is not worth solving, and it will keep producing plausible work indefinitely if nobody asks.
- **Improvement**: Two. (1) Count the rounds. A third adversarial pass on the same subject is a signal in itself, independent of what it finds. (2) Price the mechanism against what it governs, periodically and out loud. The loop's cost per rule change was visible in its own artefacts from the second cycle onward — five revisions, twenty-four dispositions, one accepted rule reaching a live artefact across four assays — and nobody added it up until the question was asked directly.
- **Signal**: workflow
- **Constraint**: none — the improvement is a judgement heuristic no check can evaluate, and the session just removed the apparatus that would have governed such a rule. Adding one now would be reaching for the machinery being retired.
- **Session metadata**:
  - Duration: ~8 hours across the full session, ~3 hours since the previous reflection
  - Model tiers used: capable (100%) — Opus 5 throughout, main loop and all nine subagent dispatches
  - Pipeline stages completed: manual sequence — /harness-assay, /diaboli x3, /choice-cartograph, /reflect x2; no orchestrator run
  - Agent delegation: partial
