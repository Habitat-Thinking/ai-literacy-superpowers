- **Date**: 2026-05-11
- **Agent**: assessor (via /assess command); claude-opus-4-7[1m] main session; no subagent dispatches
- **Task**: Ran the quarterly AI literacy assessment (third L5 continuation, anchored at the 90-day quarterly cadence declared in CLAUDE.md). Produced `assessments/2026-05-11-assessment.md`. Walked five workflow recommendations interactively (R1 cost-capture, R2 SessionStart hook for AGENTS.md read-back, R3 harness-audit refresh, R4 sustainable-pace snapshot field, R5 portfolio-adoption snapshot field) — all five accepted. R4 and R5 were applied inline as edits to `snapshot-format.md` and `harness-health.md`; R1 and R3 are next-actions in this quarterly sitting; R2 is filed as a follow-up PR with its own spec.
- **Surprise**: Three things. **(a) Drift on entry to /assess was immediate, mechanical signal.** The README Skills badge was stale by one (31 vs actual 32). The HARNESS Status block claimed 18/18 GC rules active when 25 are now declared. The AI Literacy badge still linked to the 2026-04-28 assessment. None of this required interpretation — the artefacts publish their counts in machine-checkable form, and the gap between declaration and reality was visible within seconds. **The L5 epistemic gain is not "knowing the system is healthy" — it is knowing the system has drifted before any human had to notice.** That is the move every L5 maturity claim leans on. **(b) The TDAD pillar followed the same arc as the governance subsystem did six weeks ago.** Design spec → scaffolding → HARNESS constraints → CI gates → orchestrator integration → docs site section → ONBOARDING regen. Two iterations of the arc make it a pattern; the pattern is now tacitly load-bearing on how this project ships new pillars. Worth naming explicitly somewhere — possibly in AGENTS.md ARCH_DECISIONS — so the next pillar (whenever it arrives) inherits the shape rather than rediscovering it. **(c) Cost capture has been flagged in three consecutive assessments and the gap remained the same: not tool friction, not insufficient design, just no calendar anchor.** Today's assessment is itself the calendar anchor — the gap exists in the form "we never run the tool" and the only fix is "run the tool today, then again next quarter." The L5 framework's logic is: at this level, infrastructure outpaces habit; closing the seam is operational discipline, not architecture.
- **Proposal**: Two for the next AGENTS.md curation pass. **(1) AGENTS.md ARCH_DECISIONS — "shipping a new platform pillar follows a six-step arc":** the arc is *design spec → scaffolding → HARNESS constraints → CI gates → orchestrator integration → docs site section + ONBOARDING regen*. Two reference instances: governance (Apr 2026) and TDAD (May 2026). Future pillars should inherit this shape rather than rediscovering it. Tier: ARCH_DECISION, recurring pattern. **(2) AGENTS.md STYLE — "an assessment is the calendar anchor for the operations it depends on":** /assess at L5 is no longer about determining level; it is the quarterly sitting at which /cost-capture, /governance-audit, and /harness-audit also run. The assessment's value at L5 is procedural, not diagnostic. Tier: STYLE.
- **Improvement**: Two improvements worth surfacing. **(1) The "Auto-updated by /harness-audit — do not edit manually" contract on HARNESS.md Status creates a small dilemma during /assess.** The /assess command spec says "if Status counts don't match actual declarations, update them"; the HARNESS comment says "do not edit manually." Today's run resolved this by deferring to /harness-audit as a workflow recommendation (R3) — a clean separation, but it does mean the assessment doc cannot complete the Status fix on its own. A possible refinement: have /assess dispatch /harness-audit at the start of Phase 6 (immediate adjustments), so Status is fresh before the assessment doc references its counts. **(2) The literacy-improvements skill correctly refused to invent a fictional L6.** The framework tops out at L5, and the skill says so. At L5, the substitute lever is Phase 7's workflow recommendations — operational consolidation rather than level progression. This is correct framework discipline but it leaves /assess's Phase 7b as a near-empty step at L5. Worth considering whether Phase 7b should become "operational consolidation pass" at L5, with its own checklist (cost capture cadence, AGENTS.md read-back, depletion management) rather than a generic skill invocation. Today's R1–R5 effectively performed this role manually; codifying it would standardise the L5 case.
- **Signal**: workflow + context
- **Constraint**: none — the recommendations are operational habits, not falsifiable enforcement. R2 (SessionStart hook for AGENTS.md read-back) may, if implemented, ship with its own constraint via /harness-constrain during the follow-up PR.
- **Session metadata**:
  - Duration: ~75 min (habitat discovery → broad signal scan → 5 clarifying questions → assessment document authoring + validation → README badge fixes → snapshot-format edits → reflection authoring).
  - Model tiers used: claude-opus-4-7[1m] throughout; no subagent dispatches. The assessor skill ran inline rather than via the dedicated subagent.
  - Pipeline stages completed: discovery → scan → questioning (5) → assessment doc → validation → immediate adjustments (README) → recommendations (5 accepted) → improvement-plan skill (returned N/A — correct refusal) → reflection → README/HARNESS commit-stage edits → PR ceremony.
  - Agent delegation: manual — direct edits, no subagent dispatches. The assessor skill ran inline in the main session.
