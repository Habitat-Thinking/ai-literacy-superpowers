- **Date**: 2026-08-13
- **Agent**: Claude Opus 5 (orchestrated manually across the full pipeline)
- **Task**: Built the Cadence Sentinels epic S1–S7 (four new sentinels — coda, mast, wip-warden, convener — plus the Diaboli's embedded-assumptions hunt-list, five hooks, three record contracts, two deterministic constraints), then closed seven follow-ups and audited the sentinel docs.
- **Surprise**: Three times, a spec asserted an existing repo convention that the adversarial gate falsified by opening the file that defines it. S6 claimed "all six objection categories interrogate the spec" — the shipped code-time weighting already directs `risk` at "specific evidence from the implementation". S7 claimed a one-page-per-sentinel convention and produced a five-row table proving it — the table was built by omitting `decision-discipline-triad.md` (covers three) and `the-cost-estimation-loop.md` (a second cost-estimator page). The #499 spec classified parking records as operational state — `sentinel-design/SKILL.md` names them by name on the *by the person* side, and closes with "when in doubt, this category does not apply". Each premise was load-bearing for a whole slice, and **two of the three had already been put to the human as a gate decision**, so the human decided on a false input. The information was never missing; it was unread. A second, related shape appeared four times: over-strict extraction patterns producing confident false findings — a regex requiring backticks that were not there, a grep matching agent-name strings instead of concepts, a mutation anchor landing in a comment, a `[] or X` that evaluates to X. Each looked like a real result until checked against a file already known.
- **Proposal**: AGENTS.md STYLE — "a claim about an existing convention is sourced from the file that defines it, never from the cases that fit." The failure is not ignorance; it is generalising from a sample when the definition is one grep away. Sibling to the promoted "harness artefacts derive from the source of truth" decision, applied to prose rather than to checks.
- **Improvement**: The adversarial gate caught all three, but only after the spec was written and the human had answered. Moving the obligation to the spec author — cite the defining file when asserting a convention — shifts the cost from post-hoc to up-front. Separately: every derived check written this session (roster parity, docs coverage, hooks parity, usage path) reads its relation from *both* ends, and each was mutation-tested against the drift shape that had actually occurred. That pairing — derive from both ends, then mutate against the real historical failure — worked better than any review step and is cheap enough to be routine.
- **Signal**: workflow
- **Constraint**: Specs cite the source of a claimed convention (agent)
- **Promoted**: 2026-08-13 → HARNESS.md: Specs cite the source of a claimed convention
- **Session metadata**:
  - Duration: ~2 days of session time (2026-08-08 → 2026-08-13, resumed across compaction)
  - Model tiers used: capable (100%) — Opus 5 throughout, including all advocatus-diaboli dispatches
  - Pipeline stages completed: spec-writer, advocatus-diaboli (spec gate, 5 slices), implement, integration — TDD applied per slice; carpaccio and choice-cartographer not dispatched, the build spec supplying the slicing
  - Agent delegation: partial
