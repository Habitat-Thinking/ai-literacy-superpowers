- **Date**: 2026-06-22
- **Agent**: claude-opus-4-8[1m] (main conversation; spec-writer / tdd-agent / code-reviewer / integration-agent + carpaccio subagents per slice)
- **Task**: Sliced the Dynamic Workflows Alignment epic (D1–D9) via carpaccio into 7 issues (#438–#444), then drove S1 (#438, foundational `dynamic-workflows` skill + election discipline, v0.58.0) and S2 (#439, four workflow templates + the INV-1/INV-2 firewall, v0.59.0) through full spec-first pipeline; plus two planning/clarity PRs foregrounding the Claude-Code-only runtime scope across the slices, the umbrella spec, and the shipped skill (#448, #449).
- **Surprise**: The adversarial code-review on S2's firewall (`scripts/inv-firewall.sh` — INV-1's deterministic teeth) was where the slice earned its keep. The first matcher passed all fixtures green yet had three exploitable bypasses the *tests did not catch*: an INV-1 write hidden by string concatenation (`"AGENTS" + ".md"` never forms the literal `AGENTS.md`), an INV-2 high-privilege tool that escaped via casing (`Bash` ≠ `bash`), and a high-priv tool on a continuation line of a multi-line `@tools` list. The fix — matching the durable *stem* as a whole word in comment-stripped code (Option C), `tolower` on tool values, list accumulation, and case-insensitive markers — only emerged because the reviewer was told to *try to break it*, not to confirm it. A green test suite is necessary but proves only the cases you imagined; the firewall is exactly the artefact where the unimagined case is the threat. Separately, CI caught what local runs did not: the `Shell scripts use strict mode` constraint greps the **first 15 lines** for `set -euo pipefail`, and a Knuth-style literate preamble (here ~50 lines) pushes it out of the window — literate-header discipline and a head-anchored strict-mode check are in direct tension.
- **Proposal**: AGENTS.md GOTCHA — "A deterministic security gate must be adversarially probed, not just unit-tested green. For a grep/awk firewall: match the *stem/intent* not the literal surface (a name split by concatenation/template-literal/array-join still carries the stem token); normalise case on both *markers and values*; accumulate multi-line declared lists and *fail closed* on an unterminated one. Encode each closed bypass as a named red-bar fixture so the regression can't silently reopen." And: "Shell scripts with a literate preamble must still place `set -euo pipefail` immediately after the shebang — the `Shell scripts use strict mode` constraint only reads the first 15 lines; the preamble goes below strict mode, not above it."
- **Improvement**: When authoring a new deterministic gate, dispatch the code-reviewer in explicit break-it mode (enumerate evasions: split/encode/case/whitespace/order) as a standard step, and ship the gate with a fixture per evasion class. Consider a tiny lint that asserts `set -euo pipefail` is within the first N lines of every committed `*.sh` at author time (PreToolUse) so the constraint surfaces before CI, not after a failed merge.
- **Signal**: success
- **Constraint**: none new (reinforces the existing `Shell scripts use strict mode` PR constraint and the `Layer 0 bash tests run on macOS and Linux` portability constraint — the firewall matcher was written POSIX-only for exactly that reason)
- **Session metadata**:
  - Duration: long single session spanning the epic slicing + S1 + S2 + three clarity/patch PRs (#445, #448, #449, #446, #450)
  - Model tiers used: claude-opus-4-8[1m] for the main driving/design loop and all pipeline subagents (spec-writer, tdd-agent, code-reviewer ×2, integration-agent ×2, carpaccio)
  - Pipeline stages completed: per slice — carpaccio (epic) → spec-writer → human GATE → tdd-agent (red) → implement → code-reviewer (adversarial, re-review on S2) → integration-agent (PR/CI/merge); human-adjudicated the GATE decisions (D3 threshold, D4 routing, D6 staging, S2 firewall mechanism)
  - Agent delegation: high — every pipeline stage delegated to its specialised subagent; main loop drove implementation, version plumbing, and the firewall hardening after review
