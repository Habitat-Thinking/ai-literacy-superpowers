- **Date**: 2026-06-15
- **Agent**: claude-opus-4-8[1m] main session, manual orchestration. Per slice: a spec-mode `advocatus-diaboli` (×3), the `code-reviewer` (×3), and a `cost-estimator` (general-purpose carrying the agent charter); orchestrator not invoked.
- **Task**: Drove the full cost-estimation chain — #411 (filed from a prior reflection) → #412 family matching (v0.50.0, PR #412) → #413 capture-time binding-gap advisory (v0.51.0, PR #415) → #414 single-source stems + deterministic drift guard (v0.52.0, PR #416) — each spec-first → spec-mode diaboli → implement → code-review → green-CI → merge, plus the spec-status chores (#407-style, PR #417).
- **Surprise**: Across **all three** implemented slices the spec-mode diaboli **refuted or reshaped my *recommended* design every time — and twice the recommended *mechanism* was wrong, not just the wording.** #411's recommended naïve cross-tier proxy reused `cost_basis: snapshot-actuals` (a contract lie a consumer can't distinguish from a real grounding) **and** collapsed the existing split-tier-spread validator (2 criticals) — it would have shipped records the validator rejects. #414's recommended snapshot-content GC staleness rule false-positived on **every legitimate cheap-tier-only month** and false-negatived the **actual feared case** (a staggered rollover where one stem still resolves), and the harness-gc agent had no stem logic to run it (5 highs — refuted outright). Both times the human disposed a **pivot** away from what I'd recommended (#411 → engineered Option B′ with an additive `snapshot-actuals-proxied` enum + a split-tier exemption; #414 → a deterministic mention-consistency check instead of the GC rule). The pattern is consistent enough to be **predictive**: for honesty-burdened, contract-laden cost work, my first-pass "recommended option" was systematically over-confident, and the diaboli — not my own authoring pass — was the design-correction mechanism.
- **Proposal**: For a human to decide. An AGENTS.md WORKFLOW/STYLE note: **a spec's "recommended option" is a hypothesis to be stressed by the spec-mode diaboli, not a default to be confirmed — most acutely when the slice carries an honesty burden (predicting or pricing) or touches a closed contract (a validator checklist, an enum like `cost_basis`).** Corollary: when the diaboli lands a critical/high *on the recommended path itself*, **surface the genuine fork to the human** rather than absorb-and-proceed — both pivots here (#411, #414) produced materially better, more honest designs than the recommendation would have. (This sharpens, for the cost/contract domain, the existing spec-first + diaboli discipline; the new content is "hold the recommendation loosely and route a refuted-recommendation to a human fork.")
- **Improvement**: Lead each cost/contract spec's recommended option with an explicit "diaboli will stress this" framing and pre-list the contract surfaces it could break (the validator lines, the enum, the grounding states) so the diaboli's job is scoped and the criticals surface in one pass. Secondary/mechanical: two preventable gotchas recurred and were caught *late* (by self-check / CI, not avoided): (1) the one-liner `open(p,"w").write(open(p).read().replace(...))` **truncated `plugin.json` to empty** because the write-mode `open` truncates the file *before* the argument's read runs — use `Edit`, or read into a variable first; (2) markdownlint **MD018** fired when line-wrapping placed a `#<issue>` reference at the start of a line (read as a malformed ATX heading) — keep issue refs off line starts in wrapped prose.
- **Signal**: workflow
- **Constraint**: none — the two mechanical gotchas are already caught by existing gates (markdownlint / the "Consistent markdown formatting" constraint catches MD018; `Version Check` parses `plugin.json` and `marketplace.json`, so an emptied/invalid file fails CI), and both were caught before merge; no new gate is warranted.
- **Promoted**: 2026-06-15 → AGENTS.md STYLE: "a spec's recommended option is a hypothesis for the spec-mode diaboli to stress, not a default to confirm; surface a critical/high-on-the-recommended-path fork to the human"
- **Session metadata**:
  - Duration: long multi-hour session (2026-06-15); 5 merged PRs across the chain (#412, #415, #416, #417, plus the #408/#409 tail from the prior arc) — the cost chain itself was #412/#415/#416/#417.
  - Model tiers used: claude-opus-4-8[1m] for the main driving/design loop throughout; dispatched subagents per MODEL_ROUTING (`cost-estimator` → Standard; `code-reviewer` and `advocatus-diaboli` → Most capable). Main-loop-dominant; exact split unknown.
  - Pipeline stages completed: not an orchestrator run — manual per-slice spec-first: spec (commit 1) → **spec-mode diaboli** → implement (+ Layer-1 structural test / TDAD scenario) → dispatched code-review → manual integration (version triplet, CHANGELOG, PR, CI, merge).
  - Agent delegation: manual — orchestrator not invoked; advocatus-diaboli / cost-estimator / code-reviewer dispatched individually per slice, everything else driven by hand.
