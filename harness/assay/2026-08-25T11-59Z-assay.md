---
assay: harness/assay/2026-08-25T11-59Z-assay.md
date: 2026-08-25
agent: harness-assayer
model: claude-opus-5
---

# Assay — the first run of the governed loop against itself, and the wave that followed

Second assay this repository has run. The first is
`harness/assay/2026-08-25T08-08Z-assay.md`, with corrections at
`harness/assay/2026-08-25T08-08Z-assay.errata.md`. Nothing below is carried
forward from it; where a prior finding is cited, the underlying artifact was
re-read at `b1982b0`.

Phase under assay: `2e2d1fa` (#546) through `b1982b0` (#568) — twelve merged
PRs, of which ten landed on 2026-08-25.

## Sources

Read (`observed`):

- `HARNESS.md` at `b1982b0` (1175 lines; 36 `### ` constraint headings in
  `## Constraints`, 19 GC rules), `AGENTS.md`, `CLAUDE.md`, `CHANGELOG.md`
- `harness/decisions/` — `README.md`, `index.md`, and **three** decision
  records: one `rejected`, one `accepted`/`retirement`, one
  `accepted`/`superseded`
- `harness/enforcement-report.md`, `harness/surfaces.yaml`
- `harness/assay/2026-08-25T08-08Z-assay.md` and its `.errata.md`
- `ai-literacy-superpowers/commands/harness-*.md`,
  `agents/harness-*.agent.md`, `skills/harness-assay/SKILL.md`,
  `scripts/harness-registrar.py`, `scripts/check-harness-decisions.py`
- `.github/workflows/harness.yml`, `.github/workflows/gc.yml`
- `docs/superpowers/specs/` (seven new specs dated 2026-08-25),
  `objections/`, `stories/`, `consultations/`, `parked/`,
  `harness-evolution-build-spec.md`, `harness-evolution-build-spec-claims.md`
- `docs/plugins/ai-literacy-superpowers/` — reference, how-to and tutorial
  pages for the loop
- `git log` for `2e2d1fa~1..b1982b0`, full commit bodies, and per-file history
- GitHub Actions: run lists for *Harness Constraints* and *Garbage Collection*,
  and **per-step conclusions** via `gh api .../jobs` for runs `32836457544` and
  `32839441437`
- `REFLECTION_LOG.md` — last dated entry 2026-08-14 (`reported` as to content,
  `observed` as to date)
- `observability/snapshots/` (latest `2026-07-21-snapshot.md`),
  `observability/governance/` (latest `audit-2026-06-13.md`)
- Two read-only validator runs on the clean tree: `check-harness-decisions.py`
  reported `harness decisions: OK (3 HDR(s) checked)`;
  `harness-registrar.py check` reported `harness check: OK (3 decision(s))`.
  `git status` was empty before and after.

Absent, not empty:

- **The Mast boundary-note store** — `$HOME/.claude/mast` does not exist on
  this machine. I can make no claim about whether a boundary notice fired
  during this phase. "Nothing fired" and "nothing was recorded here" are
  different facts and I observed only the second.
- **`.claude/settings.local.json`** — not present in this working tree, so the
  per-machine hooks `CLAUDE.md` describes are not installed here.
- **A seventh *Garbage Collection* scheduled run.** The workflow is weekly and
  last ran 2026-08-24. Nothing has run it since the phase began, so I make no
  claim about its current behaviour beyond reading the file.

Present and holding no records:

- `docs/superpowers/parked/` — `README.md` only.
- `docs/superpowers/consultations/` — `README.md` only.

Note on `model`: the runtime identifies this model as `claude-opus-5` in its
1M-context variant. The frontmatter carries the model name, matching the prior
assay, because a bracketed variant tag is not valid in that position.

---

## Executive summary

The phase was effective in an unusual way: the loop was pointed at itself,
broke, and the breakage was fixed rather than argued away. Running
`/harness-assay` → `/harness-propose` → `/harness-accept` once surfaced a
defect that appended a markdown region to `.github/workflows/gc.yml` and left
the weekly workflow unparseable while `/harness-check` reported `OK`
(`observed`, `harness/decisions/HDR-2026-08-25-retire-periodic-check-suite-runner.md`).
Seven further defects followed from the same run. Each got an issue number, a
spec citing a retrievable provenance, and a Layer-0 suite, and all ten PRs
merged with every check reporting `pass` (`observed`, `gh pr checks`).

The single most important opportunity is that **the code moved eight times and
the prose that tells an agent how to drive it moved zero times.**
`harness-registrar.py` gained `--reject`, `--reason-file`,
`--acknowledge-correction`, `--target` and a `correct` subcommand across #562,
#566 and #568. Not one of those strings appears anywhere under
`ai-literacy-superpowers/commands/`, `agents/` or `skills/` (`observed`,
`grep -rln`). The last change to `commands/harness-propose.md` or
`commands/harness-accept.md` was 2026-08-23. The mechanism built to stop a
corrected assay finding from propagating is invisible to the skill whose job is
to read prior assays.

That is the shape this assay exists to catch: not a rule that is wrong, but a
capability that exists in code and cannot be reached from the prompt that is
supposed to reach it.

## What worked

**The loop was run against itself, and what broke was recorded rather than
tidied.** `c35a992` (#550) is titled "Run the harness evolution loop for the
first time, and record what broke". Its body states that acceptance corrupted
`gc.yml` and that `/harness-check` reported `OK` against the corrupted file
(`observed`). The corruption is now absent — `grep 'BEGIN GENERATED'
.github/workflows/gc.yml` returns nothing (`observed`) — and it was removed by
supersession rather than by editing the frozen record.

**The retirement record argues against the cheaper fix in writing.** Its
`## Rejected alternatives` names "leave the rule in force and revert `gc.yml`
by hand" and rejects it: "The record would stay `accepted` and `in force`, so
the next `/harness-compile` reapplies the region and breaks the workflow again
… A landmine with a ninety-day fuse" (`observed`). A record that states why the
convenient repair was refused is the opposite of the failure this agent's
honesty rule is written against.

**The append-only property held under pressure, and was measured rather than
asserted.** `harness/assay/2026-08-25T08-08Z-assay.md` has exactly one commit
in its history — `c35a992` (`observed`, `git log -- <path>`). Two confirmed
errors in it were corrected in a sibling `.errata.md` added two commits later
in `b7a24fb`. The assay was not touched.

**A declined finding is now a record, and the approver's words are verbatim.**
`HDR-2026-08-25-an-epic-s-authority-cited-to-a-document-nobody-else-can-open`
carries `status: rejected` and a `## Rejection` reading, in full: "My feeling
was that the rule was pointed at the wrong problem." (`observed`). It carries
no `cost` key and no tier-2 sections, which is the shape #562 specified.

**The defect behind assay-1 finding-1 was fixed even though the rule was
declined.** `docs/superpowers/harness-evolution-build-spec.md` (24 KB) and
`harness-evolution-build-spec-claims.md` now exist, and all seven specs dated
2026-08-25 carry a `**Provenance:**` line naming an issue number, another spec
by path, or a repository artifact (`observed`, `grep -n 'Provenance'`). The
behaviour the declined rule was aimed at did not recur in the phase that
followed the decline. Whether that generalises is a separate question; what is
observed is that the transcription fix travelled and the rule did not need to.

**Every substantive PR in the window shipped a spec as its first commit.**
`Check spec-first commit ordering` reported `pass` on all ten (`observed`,
`gh pr checks`), and the seven new spec files exist on disk. #565 carried the
`fix` label and #563 the `chore` label; the rest satisfied the gate with an
actual spec rather than an exemption.

## What created friction

**Documented behaviour that the code could not perform, shipped and
CI-green for two days.** Two instances, both introduced by the same commit
`1cccbf1` (#542, 2026-08-23):

- `docs/plugins/ai-literacy-superpowers/reference/harness-decision-records.md`
  stated "It binds at acceptance rather than at proposal because the Assayer
  frequently identifies a behaviour without knowing which of four agent files
  should own it. That is the human's decision, made at the gate beside the
  cost." `harness-registrar.py`'s `accept` subparser had no `--target`
  argument until `b1982b0` (`observed`, `git show b1982b0^:...` compared
  against `grep -n 'add_argument("--target"'`). The CHANGELOG for 0.86.0 states
  the consequence: "The documented workflow was therefore impossible."
- `effective_routes`' docstring read "The routing table in force, defaults
  included" while its final statement was
  `return {k: v for k, v in routes.items() if …}` — the project's routes only
  (`observed`, `git show 2806ee8^:...`). Fixed in `2806ee8`.

Impact: the second failure surfaced at acceptance, on a record that had already
been argued and costed. Both survived the spec gate, markdownlint, the
reference-page parity check and the Layer-0 suites.

**Seven minor version bumps in one working day.** `0.79.0` → `0.86.0`, six of
them fixes to a mechanism released two days earlier (`observed`, `CHANGELOG.md`
and `plugin.json`). An adopter who pulled `0.79.0` received a loop whose accept
path appends markdown to whatever artifact a record names, including YAML. That
is stated plainly in the 0.80.0 entry, which is to the repository's credit; the
friction is that the release cadence gave no window in which to notice.

**An adversarial review that changed a governance outcome is recorded entirely
as `pending`.** `docs/superpowers/objections/harness-provenance-citation.md`
holds twelve objections, all with `disposition: pending` and
`disposition_rationale: null` (`observed`, 12 of 12). The `#550` commit body
says three were verified by hand (`reported`). The finding was declined. A
reader of that record cannot tell which of the twelve carried the decision, and
the rejection record says only that the rule was pointed at the wrong problem.

**The Assayer's own evidence pool does not know that corrections exist.**
`ai-literacy-superpowers/skills/harness-assay/SKILL.md` lists "Prior assays in
`harness/assay/`" and contains no occurrence of "errata" or "correction"
(`observed`). Neither does `commands/harness-assay.md`,
`agents/harness-assayer.agent.md`, or the how-to page `assay-a-phase.md`. This
assay read the errata because the task message named it.

**Nothing was learned in writing.** `REFLECTION_LOG.md`'s last dated entry is
2026-08-14 (`observed`), across twelve merged PRs including the first full
exercise of the governance mechanism. The "what people noticed at the time"
half of the evidence pool is empty for this phase too, so every finding below
rests on artifacts.

## Findings

### finding-1 — The prose that drives the loop is never checked against the loop's code

`harness-registrar.py` accepts the long options `--acknowledge-correction`,
`--approver`, `--assay`, `--correction-file`, `--cost-file`, `--finding`,
`--hdr`, `--now`, `--reason-file`, `--reject`, `--root`, `--slug`, `--target`,
`--today`, and the subcommands `accept`, `check`, `compile`, `correct`,
`index`, `lint-assay`, `precheck`, `propose`, `review`.

`ai-literacy-superpowers/commands/harness-propose.md` names `--assay`,
`--finding`, `--slug`. `commands/harness-accept.md` names `--hdr`,
`--cost-file`, `--approver`, `--now`. A `grep -rln` across
`ai-literacy-superpowers/commands/`, `agents/` and `skills/` for
`--reject`, `--acknowledge-correction`, `--correction-file`,
`harness-registrar.py correct` and `accept … --target` returns **no files**.
Both command files were last modified on 2026-08-23; the four capabilities
landed on 2026-08-25 in #562, #566 and #568.

The divergence is not only omission. `harness-propose.md`'s step-5 validation
checkpoint requires, as **P1**, that the written record's frontmatter carries
`proposed_cost` and `cost`, and as **P5** that a tier-2 classification carries
all four extra sections. The rejection record shipped by #562 and written by
#563 carries neither key and none of those sections, by design. An agent
following the checkpoint as written against a correctly-formed rejection would
report a defect that is not there — and the command's own instruction for that
case is "Report the deviation as a defect … Report, do not patch."

The same divergence reaches the reference layer: the errata mechanism is
documented in `docs/.../reference/assay-finding-format.md` and in the
`harness-registrar.py` source, and nowhere in the agent-facing prose that
`/harness-assay` and `/harness-propose` actually load.

```yaml
classification: script-validator
enforcement: validated
surfaces: [ci]
priority: P1
evidence:
  - ai-literacy-superpowers/commands/harness-propose.md
  - ai-literacy-superpowers/commands/harness-accept.md
  - ai-literacy-superpowers/skills/harness-assay/SKILL.md
  - ai-literacy-superpowers/scripts/harness-registrar.py
  - docs/plugins/ai-literacy-superpowers/reference/harness-decision-records.md
  - HARNESS.md
overfitting_risk: low
```

**Why this layer, and why no existing owner absorbs it.** Three constraints sit
nearby and none of them fires here. *Docs site kept current* is triggered by a
PR that "adds, removes, or substantially changes a skill, agent, or command" —
and no file under `ai-literacy-superpowers/commands/`, `agents/` or `skills/`
changed in this entire phase (`observed`, `git diff --name-only`), so its
trigger condition is precisely what went missing. *Docs propagation when
shipping new commands* is scoped to a PR that adds a **new** command. The GC
rule *Command-prompt sync* compares `commands/` against `.github/prompts/` —
two prose surfaces against each other, never against the code. `/harness-audit`
would correctly report all three compliant, which is why this is a finding and
not a rejected candidate.

I am deliberately **not** proposing "update the command prompt". Four
capabilities diverged in a single day across three PRs, each written carefully
and each reviewed; the failure is not carelessness but the absence of anything
that reads a parser and a prompt in the same breath. That is a check, so the
classification is `script-validator`, which routes to `HARNESS.md`. I name no
`target` for that reason: the route wins in `target_of`, and since #568 a
routed classification refuses one.

`enforcement: validated` states the intent. No such check exists today, so the
first `/harness-compile` after acceptance should report an enforcement **gap**
on `ci` rather than `validated`. That is the enforcement report doing its job
and is not a reason to declare `advisory` instead.

**Overfitting risk: low.** The rule states a property of any command that
delegates to a script and encodes none of the four options that happen to have
diverged. It would have fired on all four independently.

**Validation plan.** Reconstruct the tree at `b1982b0^` for #562, #566 and #568
and assert the check fails on each. Assert it passes at `2e2d1fa`, where the
prompts and the parser agreed. Then assert it passes at `b1982b0` **only after**
the prompts are updated — if it passes on the current tree unchanged, the check
is not reading what it claims to read and should be refused.

#### Proposed rule

````markdown
- **Rule**: Every long option and every subcommand that a script accepts must
  appear verbatim in at least one file under
  `ai-literacy-superpowers/commands/`, when that script is invoked by a
  command in that directory. A capability the code exposes and no command
  prompt names is a capability the agent driving that command cannot reach,
  and a validation checkpoint written against an older interface reports a
  correct artifact as a defect — which is worse than silence, because the
  command instructs the agent to report rather than patch. An option whose
  only caller is another script is exempt by carrying an `# undocumented:`
  comment on the line above its `add_argument` call, so the exemption is
  visible where the option is declared rather than in a list somewhere else.
  This does not reach reference or how-to pages; those are governed by
  *Docs site kept current*, whose trigger is a change to a command, skill or
  agent file.
- **Enforcement**: deterministic
- **Tool**: `python3 ai-literacy-superpowers/scripts/check-command-cli-parity.py`
  (CI: `.github/workflows/harness.yml`)
- **Scope**: pr
````

#### Cost estimate

One-off: writing the parity check — an argparse walk over each script named in
a command file, matched against that file's text — perhaps an hour, plus the
Layer-0 suite. Then a backlog: the four capabilities already divergent must be
written into `harness-propose.md` and `harness-accept.md` before the check can
go green, which is thirty to sixty minutes of prose someone has to actually
think about, because `--reject` changes what step 5's checkpoint should assert.

Recurring: one line of prose per new option, paid by whoever adds the option,
in the same PR. For a repository that added four in a day that is not nothing;
for a repository that adds one a month it is invisible. The honest description
is that it makes adding a flag slightly more expensive and makes an unusable
flag impossible.

Three ways it can be gamed, in descending order of likelihood. **Naming the
flag and not explaining it** — a bare `--reject` mentioned in a code fence
satisfies a literal-string check while the prompt still never tells the agent
when to use it. This is the likely evasion and the check cannot see it; the
mitigation is that the flag at least becomes discoverable, which is strictly
better than today. **`# undocumented:` as a default** — the exemption is one
comment away and an author in a hurry will reach for it; it is deliberately
placed at the `add_argument` line so it shows up in the diff beside the option,
where a reviewer is already looking. **Splitting the parser** — moving options
into a wrapper script that no command file names. That would be conspicuous and
costs more than writing the line.

What the rule does **not** catch is the friction that motivated half its
evidence: a reference page or a docstring asserting behaviour the code lacks,
as in #567 and #568. I could not state a mechanical test for that class whose
false-positive rate I could predict, and proposing an advisory rule I cannot
falsify would be proposing to look comprehensive.

---

### finding-2 — A failing step still cancels every check after it, and on 2026-08-25 it cancelled the two that govern governance

`.github/workflows/harness.yml` — the PR gate — runs eight constraint steps
sequentially. Only the final `Summary` step carries `if: always()` (`observed`,
lines 25–81). The last two constraint steps, in order, are
*Harness decision records are well-formed* and
*Harness governance is applied and undrifted*: the two validators that hold the
identifier grammar, the required fields, the two-assay promotion threshold, the
three-per-cycle cap, the frozen-record check and the expiry check.

Being last, they are the most maskable steps in the file. That is not
hypothetical. On run `32836457544` (branch `harness-write-path-integrity`,
PR #552) and again on run `32839441437` (branch `harness-validator-binding`,
PR #561), step 8 *Constraint: ShellCheck compliance* is recorded `failure` and
steps 9, 10 and 11 — *Sentinel integrity*, *Harness decision records are
well-formed*, *Harness governance is applied and undrifted* — are all recorded
`skipped` (`observed`, `gh api .../actions/runs/<id>/jobs`, per-step
conclusions). Both of those PRs were changing
`ai-literacy-superpowers/scripts/check-harness-decisions.py` and
`harness-registrar.py`. The two checks that exist to catch a governance
regression did not run on two of the PRs that rewrote the governance code. Both
PRs later merged with every check `pass`, so nothing shipped unverified — but
that was a re-push, not the gate working.

`.github/workflows/gc.yml` is unchanged in this phase and has the same shape:
eleven `GC:` steps, one `if: always()`, on `Summary` (`observed`). The
*Garbage Collection* schedule has failed six consecutive runs, 2026-07-20
through 2026-08-24 (`observed`, `gh run list`). It has not run since, so I make
no claim about a seventh.

Assay 1's finding-2 observed this on `gc.yml`. Its errata narrowed the count of
declared rules the workflow covers and states that "The masking observation - a
failing step silencing every later step - is unaffected and stands"
(`observed`). Its record was accepted, corrupted its target, and was retired;
the retirement's `## Rejected alternatives` closes with "**Re-propose
immediately with `HARNESS.md` as the target.** Deferred, not rejected"
(`observed`). Every fact above was re-read at `b1982b0` rather than carried
forward, and the corrected count forms no part of this claim — I counted the
steps in both files directly.

```yaml
classification: script-validator
enforcement: validated
surfaces: [ci]
priority: P1
evidence:
  - .github/workflows/harness.yml
  - .github/workflows/gc.yml
  - harness/decisions/HDR-2026-08-25-retire-periodic-check-suite-runner.md
  - harness/assay/2026-08-25T08-08Z-assay.md#finding-2
  - HARNESS.md
overfitting_risk: low
```

**Why this layer, and what is different from the retired record.** The retired
record pointed at `.github/workflows/gc.yml` as its `target` and the compiler
appended markdown to YAML. `script-validator` now has a fixed route to
`HARNESS.md` (#552, and pinned in `harness/surfaces.yaml`), so the rule text
lands in a document that can hold it and the workflows are named in **Tool**,
which is where the retirement record says they always belonged. I name no
`target` in the metadata: the route wins, and since #568 a routed
classification refuses one.

`/harness-gc` still cannot own this. It runs the rules on demand and reports
each individually; what it cannot observe is the behaviour of a runner nobody
invoked. And `/harness-gc` has no visibility into `harness.yml` at all, which
is the half of this finding that is new.

**Overfitting risk: low.** The rule states a property of any enforcement
workflow and encodes neither ShellCheck, nor release-tag completeness, nor
snapshot staleness — the three rules that happened to fail. It would have
applied identically to all eight observed runs.

**Validation plan.** Push a branch with a deliberately failing early step in
`harness.yml` and assert that (a) every later step produces a conclusion, (b)
the job still concludes `failure`, and (c) no step reports `skipped` for a
reason other than an explicit `if:` condition. The third assertion is the one
that matters: a fix that makes the job green by tolerating failures is worse
than the defect and would satisfy (a) and nothing else.

#### Proposed rule

````markdown
- **Rule**: In a workflow that enforces declared constraints or
  garbage-collection rules, every enforcing step carries `if: always()`, and
  the job's conclusion is derived from the collected step results rather than
  from the first failure. A check that did not run is reported as not run —
  never omitted, and never counted as passing. This binds
  `.github/workflows/harness.yml` and `.github/workflows/gc.yml`. On the PR
  gate the two harness-governance checks are the last two steps, so any
  earlier constraint failure hides them; on the weekly job the whole value is
  the weeks nobody reads it. A run that stops partway reports a smaller world
  than it declared it would check.
- **Enforcement**: deterministic
- **Tool**: `.github/workflows/harness.yml` and `.github/workflows/gc.yml`
- **Scope**: pr
````

#### Cost estimate

One-off: `if: always()` on eight steps in `harness.yml` and eleven in
`gc.yml`, plus an aggregation step in each that fails the job when any
collected result failed. Perhaps forty minutes, once.

The recurring cost is the honest one and it is not zero, and it differs by
workflow. On the **PR gate** it makes red PRs noisier: a branch with a
ShellCheck error will now show three failures instead of one, and the temptation
is to read the extra two as noise rather than as two governance checks that were
never going to pass either. On the **weekly job** it converts one failure into
between one and ten. The six weeks of *Garbage Collection* failures already
hide an unknown number of results; whoever holds the cadence inherits a backlog
that was always there. The moment of maximum temptation is the first Monday
after the fix, and the tempting move is to demote whichever rule is loudest.

Two ways it can be gamed. **Making the job green rather than complete** —
collecting results and then not failing on them satisfies "a result for every
step" while deleting the signal entirely. This is the likely failure, and it is
why the rule names the job conclusion explicitly rather than only the steps.
**Deleting a chronically failing step from the workflow instead of retiring the
rule from `HARNESS.md`** — the rule stays declared, nothing runs it, and the run
reports a complete pass over a smaller set. *Convention parity* does not catch
this; it compares `HARNESS.md` against its three prose mirrors, not against the
workflows. Nothing currently checks a declared GC rule against a workflow step,
and this rule does not add that. It is a real hole and I am naming it rather
than widening the proposal to cover it.

---

### finding-3 — No constraint was hand-added to HARNESS.md during the phase, which is the corroboration assay 1 asked for and did not get

Assay 1's finding-3 was a `no-change` recording that `HARNESS.md`'s 36
constraints all arrived by hand edit while the decision corpus was empty, and
it named the condition that would make it corroborate: "If a second assay
observes a constraint hand-added to `HARNESS.md` after 2026-08-23 without a
decision record, this finding becomes its corroborating observation and the
two-assay threshold is met legitimately."

That condition did not occur. Diffing the `### ` headings of `HARNESS.md`
between `2e2d1fa~1` and `b1982b0` returns nothing — the constraint set is
identical (`observed`). `HARNESS.md` was touched exactly once in the phase, by
`a81c552` (#549), which added a commented-out optional `## Stakeholders` block
and moved the template marker from 0.73.2 to 0.79.0: eighteen insertions, one
deletion, no constraint text (`observed`, `git diff --stat`).

The corpus meanwhile moved from empty to three records — one `rejected`, one
`retirement`, one `superseded` — and holds **nothing in force**
(`observed`, `harness/decisions/index.md`). `harness/enforcement-report.md`
still reads "No accepted decisions." That looked like a contradiction and is
not: `harness-registrar.py` filters superseded and retirement records out of
the enforcement report before rendering, so a corpus whose only accepted
records are a retired rule and its retirement correctly reports nothing
enforced (`observed`, lines 1378–1381 and 1417).

Across the phase eight PRs changed the governance mechanism's **code** and
none changed its **rules**. I am not proposing that the code changes should
have gone through the loop: the loop governs `HARNESS.md` and `AGENTS.md`, and
treating every change to `harness-registrar.py` as a governance change would
convert a mechanism for restraining rules into a second review process for
Python. The state on the record is that the loop was exercised end to end,
produced three records, produced no rule, and coincided with zero hand edits to
the document it governs. If a third assay observes a constraint appearing
without a record, this is its corroborating observation. If it does not, this is
the evidence that nothing needed changing.

```yaml
classification: no-change
enforcement: advisory
surfaces: []
priority: P3
evidence:
  - HARNESS.md
  - harness/decisions/index.md
  - harness/enforcement-report.md
  - harness/assay/2026-08-25T08-08Z-assay.md#finding-3
overfitting_risk: low
```

#### Proposed rule

No change.

#### Cost estimate

None. The alternative — importing the 36 legacy constraints as `imported: true`
records so the corpus stops looking empty — would produce 36 records whose
evidence field cites nothing anyone observed, which is the ceremony this
mechanism exists to replace rather than reproduce. The reason to write this
finding down is that a corpus holding one rejection, one retirement and no rule
in force reads as failure at a glance, and the next reader deserves the
observation that it is the mechanism refusing things rather than the mechanism
being ignored.

---

## Rejected candidates

Each of these is real and re-observed at `b1982b0`. None is a finding, because
an existing owner already reports it.

**`HARNESS.md`'s Status block is stale, unchanged since the last assay.** It
reads "Last audit: 2026-08-13 / Constraints enforced: 33/34" against 36 active
constraint headings. Owner: **`/harness-audit`** — the block's own HTML comment
says "Auto-updated by /harness-audit — do not edit manually", and the audit
cadence is quarterly, so this is the cadence behaving as declared.

**Seven feature PRs merged with no objection record and no choice-story
record.** #552, #560, #561, #562, #566, #567 and #568 each shipped a spec and
none has a matching `docs/superpowers/objections/<spec-slug>.md`,
`<spec-slug>-code.md` or `stories/<spec-slug>.md`; none carries an exempting
label (`observed`, `gh pr list --json labels`, and directory listings). Owner:
**`/harness-audit`**, via `harness-enforcer` running *PRs have adjudicated
objections* and *PRs have adjudicated choice stories*, both `Enforcement:
agent`. Worth carrying to whoever runs it: `.github/workflows/harness.yml` runs
eight deterministic constraints and none of the agent-enforced ones, so CI
going green on these PRs never spoke to this. This is the second consecutive
assay to route this to `/harness-audit`; the recurrence is the fact, and the
remedy is running the audit, not writing a new rule.

**Twelve objections recorded `pending` on the review that decided the first
governance rejection.** All twelve dispositions in
`docs/superpowers/objections/harness-provenance-citation.md` are `pending` with
null rationale (`observed`, 12 of 12), while the finding was declined and the
`#550` commit says three critical objections were verified by hand. Owner:
**`/governance-audit`**. The constraint *PRs have adjudicated objections*
exempts PRs labelled `chore`, and #550 was labelled `chore`; the exemption was
written for maintenance work and was applied to a PR whose content was a
governance adjudication. Whether a constraint's exemption still serves its
intent is exactly the governance-audit question, and answering it here would be
me widening a rule to cover a case I found convenient.

**No reflection entry for any of the twelve PRs.** `REFLECTION_LOG.md`'s last
dated entry remains 2026-08-14 (`observed`). Owner: **`/reflect`**, backed by
the `reflection-prompt.sh` Stop hook and the GC rule *Reflection-driven
regression detection*.

**The snapshot is 35 days old and release tags are incomplete.**
`observability/snapshots/2026-07-21-snapshot.md` is the latest, past the
declared 30-day threshold; `v0.28.2` through `v0.29.1` remain untagged while
`v0.83.1`–`v0.85.0` exist (`observed`, `git tag`). Owners:
**`/harness-health`** for the snapshot, **`/harness-gc`** Mode 2 for the tags.
Both are individual rule failures, which is what those commands run and report.

**`v0.86.0` is not tagged.** Observed, and rejected on **materiality**: #568
merged minutes before this assay and nothing shows anyone looking for the tag.
An observation, not a finding.

**The how-to pages `record-a-governance-change.md` and `assay-a-phase.md` were
not updated for `--reject`, `--target`, `--acknowledge-correction` or the
errata channel.** Both were last touched 2026-08-23 (`observed`). Folded into
finding-1's evidence rather than proposed separately, and explicitly excluded
from finding-1's rule — its check reads command files against parsers, and
extending it to prose pages would be widening a burden I cannot cost.

## Unresolved questions

**Should `harness/surfaces.yaml`'s `routes:` table be under the loop it
configures?** It was hand-edited in `91cd510` (#552) to add
`script-validator: HARNESS.md` — a change that determines which artifact every
future rule of that classification lands in — with no decision record
(`observed`). The edit was correct, specified and reviewed, and I observed no
harm from it, which is why this is a question rather than a finding. But the
loop's stated purpose is that a governing document should not be the least
governed thing in the repository, and the routing table decides where governance
is written. Where that boundary belongs is a judgement for a human.

**Should a frozen record that cites a subsequently corrected finding say so?**
`HDR-2026-08-25-retire-periodic-check-suite-runner` cites
`harness/assay/2026-08-25T08-08Z-assay.md#finding-2` (`observed`), which
acquired a correction two commits later. `corrections_for` is called only from
`cmd_propose` (`observed`), so the refusal fires when a record is drafted and
never afterwards; a record proposed before the correction carries no marker and
`/harness-check` reports `OK`. The correction in this instance states the
underlying observation stands, so there is no harm to point at — which is
precisely why it is unresolved rather than a finding, and why a later assay may
be the one that meets it with harm attached.

**Was any Mast boundary notice raised during this phase?** Unanswerable from
here. `$HOME/.claude/mast` does not exist on this machine and the store is
per-machine and gitignored. I make no claim either way.

**Was `/coda` run at the close of these sessions?** `docs/superpowers/parked/`
holds its `README.md` and no records — unchanged since assay 1 (`observed`).
That is equally consistent with "no live thread was worth parking across twelve
PRs" and with "the Coda was not run". The record format still cannot
distinguish them.

**Does finding-1's evidence deserve a second, wider rule?** Four of the eight
PRs in this phase share a shape: a prose artifact asserted behaviour the code
did not have — the routes docstring, the target-binds-at-acceptance paragraph,
`has_validator`'s existence-only test, and the command prompts. I proposed a
rule for the one member of that set I can mechanically check, and said so in
the cost estimate. Whether the wider class deserves an advisory rule at the
spec gate, or whether an advisory rule nobody can falsify is worse than none, is
a judgement about what the harness is for. It belongs to the approver.
